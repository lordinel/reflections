How is the staging area different from the working directory and the repository? What value do you think it offers?

	Staging area allows you to specify which files to track for the commit. It allows you to select which files to include in the commit.

How can you use the staging area to make sure you have one commit per logical change?

	It allows me to review the files to be added in the commit before making the actual commit.

What are some situations when branches would be helpful in keeping your history organized? How would branches help?

	It allows me to test experimental features without cluttering master codes.

How do the diagrams help you visualize the branch structure?

	It allows me to see which commits are detached and which commits are reachable from other commits.

What is the result of merging two branches together? Why do we represent it in the diagram the way we do?

	The codes between two branches were combined.

What are the pros and cons of Git’s automatic merging vs. always doing merges manually?

	It is faster since it is automated. However, when there are conflicts, it halts the merging and asks the user to resolve the conflict manually.
